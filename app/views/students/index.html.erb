<h1> Students </h1>

<% @students.alpha_order.each do |student| %>
  <div id="students-<%= student.id %>">
  <p>Name: <%= student.name %> </p>
  <p>Age: <%= student.age %> </p>
  <p>House: <%= student.house %> </p>
  </div>
<% end %>

<h3> Average Age: <%= @students.average_age %> </h3>